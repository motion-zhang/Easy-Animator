package cs3500.animator.controller;

// the Interface for the Animation controllor.
// Good for future Design.
// AnimationController implements this interface.
public interface IViewController {
  ButtonListener getButtonListener();
}
